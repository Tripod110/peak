# Peak progress — 2026-09-24

This follows [2026-09-22](2026-09-22.md). The full detail is in [CHANGELOG.md](../CHANGELOG.md)
(v44–v48) and [DECISIONS.md](../DECISIONS.md) (D-25).

**Status:**
- `main` is at v48.
- v47 and everything before it is live on GitHub Pages.
- v48 and these docs are pushed alongside this note.
- The Worker is deployed with the hardening from v41 (version `01e0b67e`).
- 135 tests pass.

## Done since the last report

The "still to build" list is finished.

- **v44, the engine listens:**
  - a second attempt at the old top weight after a deload, before another deload
  - back-off sets
  - a rep check in the rest dock ("All 5 reps?"), with optional Easy/Hard effort
  - a warning when volume goes past MRV (the most sets you can recover from)
  - side delts as their own muscle
- **v45, the gym floor:**
  - a warm-up ramp (40/60/80/90%)
  - supersets
  - a 1.25 kg metric stepper
  - correct rest times for different kinds of rows
  - keyboard handling, so the dock rides above the keyboard
  - the + button and toasts no longer covering content
- **v46, looks and access:**
  - 4.5:1 button contrast in every theme (measured in the browser)
  - the Light theme finally has light bars
  - 16px inputs, 44px targets, landscape safe areas
  - focus follows drill-ins, and toasts are spoken
  - a service worker that can't half-install
- **v47, onboarding leads with training:**
  - days per week and a split come first; maintenance is the default goal
  - tab order is Today · Train · Food · Sleep · Grocery
  - a 0 lb guard
  - no failure message in the first week
  - one-tap protein staples in Grocery
  - your sleep-vs-score split shown on Train
- **v48, bring your history:** import from Strong or Hevy CSV, with no duplicates and one-step undo.
- **Docs:**
  - ROADMAP.md rewritten as a development roadmap
  - PAYMENTS.md marked parked
  - D-25 records the direction change

## What's left

Everything left needs hardware or is new work (ROADMAP → *Next*):

1. **Real-device pass** (SHIPPING.md). It's never been run. The highest-value checks:
   - the keyboard with the dock
   - safe areas
   - the offline fallback
   - VoiceOver / TalkBack
2. **Goal-aware plateau engine.** On a cut, Train still calls flat lifts stalled while the coach
   calls holding strength a win.
3. Supersets saved in routines, not just in a session.
4. Deeper coach memory: following up on switch-ups, and learning preferred rep ranges.
5. Training-day and rest-day food targets.
6. Apple Health / Google Fit.
